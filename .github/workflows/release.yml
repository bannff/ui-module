name: Release

on:
    release:
        types: [published]

jobs:
    pypi-publish:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6
            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                  python-version: "3.10"
            - name: Build and publish
              run: echo "Publishing to PyPI (placeholder)"
